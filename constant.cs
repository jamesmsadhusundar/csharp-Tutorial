using System;

namespace constant
{
    class Program
    {
        static void Main (String[] args)
        {
            const String name = "james";   //const is a keyword, it declares variable as constant which is never changeable.
            
            Console.WriteLine(name);
        }
    }
}